VAR c : INT;
VAR a : ARRAY OF INT;
VAR b : INT;
VAR b : ARRAY OF INT;
VAR i : INT;
{
	t := (i + c);
	a := APP(b, 3);
	{
		{
			i := (i + 1);
			c := UPD(a, i, i)
		}
	}*;
	{
		{
			b := (a - i);
			b := (a + i);
			b := (a * i);
			b := (a / i)
		} U
		{
			c := 1;
			(a > c)?
		}
	}
}

